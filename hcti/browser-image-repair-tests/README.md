Run from the WIN5 workspace containing intrst-films-templates/, proofs/assets/ and evidence-proofs/. Scripts write image-repair/. Requires Node Playwright, Chromium at the documented path, Python Pillow and NumPy. Run render.cjs then validate.py then gates.cjs. Original assets resolve from image-binding-manifest-v2.json; fonts from proofs/assets/fonts-inline.css. No provider access.
